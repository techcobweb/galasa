#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#
name: PR build maven

on:
  pull_request:
      branches: [main]
      paths:
        - "modules/buildutils/**"
  workflow_run:
    workflows: ["PR build gradle"]
    types:
      - completed
env:
  REGISTRY: ghcr.io
  NAMESPACE: galasa-dev

jobs:
  build-maven:
    name: Build Maven source code and Docker image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'semeru'
          cache: maven

      - name: Print githash
        run: |
          echo $GITHUB_SHA > ./maven.githash
      
      # Copy secrets into files to use in workflow
      - name: Make secrets directory
        run : |
          cd modules/maven
          mkdir /home/runner/work/secrets

      - name: Copy settings.xml
        env:
          MAVEN_SETTINGS_XML: ${{ secrets.MAVEN_SETTINGS_XML }}
        run : |
          echo $MAVEN_SETTINGS_XML > modules/maven/home/runner/work/secrets/settings.xml

      - name: Copy GPG passphrase
        env:
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run : |
          echo $GPG_PASSPHRASE > modules/maven/home/runner/work/secrets/passphrase.file

      - name: Copy GPG key
        env:
          GPG_KEY_BASE64: ${{ secrets.GPG_KEY }}
        run : |
          echo $GPG_KEY_BASE64 |  base64 --decode > modules/maven/home/runner/work/secrets/galasa.gpg
            
        # Set up Maven GPG directory
      - name: Make GPG home directory
        run: |
          mkdir modules/maven/home/runner/work/gpg
        
      - name: Change directory permissions
        run: |
          chmod '700' modules/maven/home/runner/work/gpg
  
      - name: Import GPG
        run: |
          gpg --homedir modules/maven/home/runner/work/gpg --pinentry-mode loopback --passphrase-file modules/maven/home/runner/work/secrets/passphrase.file --import modules/maven/home/runner/work/secrets/galasa.gpg
        
      - name: Copy custom settings.xml
        run: |
          cp modules/maven/home/runner/work/secrets/settings.xml modules/maven/home/runner/work/gpg/settings.xml
      
      - name: Building Maven source code
        run: |
          mvn -f galasa-maven-plugin/pom.xml deploy -X \
          -Dgpg.skip=true \
          -Dgalasa.source.repo=https://development.galasa.dev/main/maven-repo/gradle \
          -Dgalasa.central.repo=https://repo.maven.apache.org/maven2/ \
          -Dgalasa.release.repo=file:${{ github.workspace }}/repo \
          --batch-mode --errors --fail-at-end \
          --settings /home/runner/work/gpg/settings.xmlmodules/maven

      - name: Build Maven image for testing
        uses: docker/build-push-action@v5
        with:
            context: .
            file: modules/maven/dockerfiles/dockerfile
            load: true
            tags: maven:test
            build-args: |
              dockerRepository=ghcr.io
              tag=main