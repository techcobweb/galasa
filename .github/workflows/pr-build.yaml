#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#
name: PR build

on:
  pull_request:
    branches: [main]

# The build jobs.
jobs:
  call-workflow-passing-data:
    uses: ./modules/buildutils/.github/workflows/buildutils.yaml
    